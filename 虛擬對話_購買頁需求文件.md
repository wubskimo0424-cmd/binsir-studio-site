# 虛擬對話 購買頁需求文件

> 給 guestars.app（授權後端）建立 `/virtualchar/buy` 用。
> 官網（guestars.tw）這側的按鈕，等你給網址後我再接上。

---

## ⚠️ 最重要的差異：這個產品「不能自動交付」

drinkcam 和 GUESTARS 都是付款後**立刻**自動交付（發授權碼 / 開房間）。
**虛擬對話不是** —— 角色需要人工建置，官網標示 3-5 個工作天。

所以付款成功後的流程是：

```
刷卡成功
   ├─ 建立訂單，狀態 = 製作中
   ├─ 寄「已收到訂單」確認信給客戶（告知工作天、附需求表連結）
   ├─ 通知阿斌有新訂單（Email / LINE）
   └─ 回綠界 1|OK
        ↓
   阿斌人工建置角色（3-5 個工作天）
        ↓
   完成後：把角色網址填進後台 → 寄交付信給客戶 → 狀態 = 已交付
```

**因此需要一個簡單的訂單清單頁**，讓阿斌看到哪些訂單還在製作中、並在完成時填入角色網址觸發交付信。這是三個產品裡唯一需要後台的。

---

## 方案表（後端寫死，不可信前端）

| plan 代碼 | 方案 | 價格 | 內容重點 |
|---|---|---|---|
| `gift` | 送禮 · 紀念版 | NT$3,800 | 1 角色、含 US$25 對話成本、網址 30 天、1 次微調 |
| `event` | 活動 · 派對版 | NT$5,800 | 1 角色、含 US$50 對話成本、網址 60 天、2 次微調、優先建置 |
| `pro` | 現場 · 公開版 | NT$12,000 起 | 1-3 角色、US$100+ 成本、網址 90 天以上、無限微調 |
| `topup` | 加購對話成本包 | NT$1,500 | 既有角色加值 |
| `extend` | 加購有效期延長 | NT$500 | 每 30 天 |

**建議**：`pro` 標示「依規模另議」，不要開放線上直接刷卡，改導到聯絡洽談（`https://guestars.tw/#contact`）。
`topup` / `extend` 是既有客戶加購，建議需填原訂單編號或角色網址。

---

## 結帳表單需要收集的欄位

**基本（付款前必填）**

- 訂購人姓名
- Email（交付信寄這裡，務必正確）
- 聯絡手機
- 方案（由 `?plan=` 帶入預選）
- 期望完成日期（用來排程，並提醒是否來得及）

**角色需求（建議「付款後」再收集，不要卡在付款前）**

理由：付款前要求上傳照片、填一堆設定，會大幅降低成交率。
建議做法：付款成功的確認信裡附一個「角色需求表」連結（Google 表單即可），請客戶補件。

需求表要問的內容：

- 角色名稱、稱呼方式
- 參考照片（1-3 張，寵物 / 人物皆可）
- 個性描述（活潑 / 溫柔 / 毒舌…）
- 說話風格、口頭禪、常說的話
- 關係設定（例如「我是主人的狗狗 Oreo」）
- 有沒有想讓它知道的專屬回憶 / 梗
- 使用場景（送禮 / 婚禮播放 / 展場）

---

## 建議的 URL 與參數

```
https://guestars.app/virtualchar/buy?plan=gift
https://guestars.app/virtualchar/buy?plan=event
```

（`pro` 導到洽談、不進結帳）

---

## 信件內容建議

**① 付款確認信（付款成功後立即寄）**

- 已收到訂單與付款，訂單編號
- **明確告知製作時間**（3-5 個工作天 / 依方案）
- 角色需求表連結（請客戶填寫，越詳細角色越像）
- 提醒：需求表填完才會開始製作
- 客服聯絡方式

**② 交付信（角色完成後手動觸發）**

- 專屬角色網址
- 有效期限與到期日（依方案 30/60/90 天）
- 使用說明（需麥克風權限、建議用手機瀏覽器開啟）
- 微調次數說明與申請方式
- 加購對話成本包 / 延長有效期的連結

---

## 視覺規範

沿用先前給的 `drinkcam_buy_設計規範.md`（米色暖橘設計系統），
header/footer、色票、按鈕、輸入框樣式完全相同，只換內容。

---

## 官網現況

`guestars.tw/virtualchar` 的方案按鈕目前指向頁內 `#order` 的表單，
該表單原本 POST 到 `/api/order/virtualchar`（不存在、會 404）。

**購買頁做好後給我網址，我會**：
1. 三個方案按鈕接到 `?plan=gift` / `?plan=event`，`pro` 接到聯絡洽談
2. 移除頁內那張會 404 的表單，改成導向結帳頁的 CTA
（與 guestars.html / drinkcam.html 已完成的處理方式一致）

---

## 補充：Demo 已在官網嵌入

官網虛擬對話頁已用 iframe 嵌入兩個示範角色（Oreo、Dan），
指向 `https://mr-yes-chat.vercel.app/oreo`。結帳頁若要放示範連結可沿用。
