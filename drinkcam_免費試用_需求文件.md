# drinkcam 免費 2 小時試用 — 自動發證需求

> 給 guestars.app 建立 `/drinkcam/trial`。
> 官網（guestars.tw）的「申請試用」按鈕已指向此路徑，**若你用不同路徑請告知**。

---

## 目標

訪客填表 → **立即自動**收到 2 小時試用授權碼（不再是「24 小時內人工寄送」）。
用途是抓潛在客戶：拿到 Email、讓對方實際體驗過，成交率遠高於純看介紹。

技術上沿用既有的 `sign-license.js` + Resend，跟正式發證同一套，只是期限短。

---

## ⚠️ 最關鍵的一點：試用時段不能從「現在」開始

官網文案是「**想去飯店或場勘地點現場測試**」——
意思是使用者**不是現在要用**，是之後到場勘現場才要用。

如果簽發 `valid_from = 現在`、`valid_until = 現在+2小時`，
使用者明天到飯店時授權早就過期了，試用直接作廢、還會來客訴。

**做法：表單必須讓使用者選「預計試用的日期與時間」**，
授權簽發成該時段起算，並建議**前後各給 30 分鐘緩衝**（實際窗口約 3 小時），
避免現場延誤導致白白浪費。

```js
// 範例
const start = new Date(chosenDateTime).getTime() - 30 * 60 * 1000;  // 提前 30 分
const end   = start + 3 * 60 * 60 * 1000;                            // 共 3 小時窗口

payload.valid_from  = new Date(start).toISOString();
payload.valid_until = new Date(end).toISOString();
```

---

## 表單欄位

| 欄位 | 必填 | 說明 |
|---|---|---|
| 名稱 / 公司 | ✅ | |
| Email | ✅ | 授權碼寄這裡，也是防濫用的識別依據 |
| **預計試用日期與時間** | ✅ | 見上方說明，決定授權有效時段 |
| 聯絡手機 | ➖ | |
| 試用場地 / 用途 | ➖ | 例：晶華飯店場勘。對你了解客戶很有價值 |

---

## 授權參數建議

| 參數 | 建議值 | 理由 |
|---|---|---|
| `tier` | `pro` | 讓對方體驗到完整功能，轉換率較高 |
| `max_phones` | `4` | 足夠體驗多機，又不足以實際辦一場活動 |
| `features` | pro 預設 | multi_view, recording, rotation, smart_filter, sticker |
| `scene` | `wedding` | |
| `customer` | `{名稱}（試用）` | 軟體上會顯示，一眼看出是試用 |
| 有效時段 | 選定時間 −30 分，共 3 小時 | 見上方 |

---

## 防濫用（必做）

- **同一 Email 只能申請一次** —— 送出前先查既有試用紀錄，已申請過就顯示提示並導到購買頁
- **記錄每一筆試用**（Email、時間、場地、license_id），方便你追蹤成效與辨識異常
- 建議再加簡易頻率限制（例如同 IP 每日上限），避免有人用多組信箱刷

> 2 小時 / 4 支手機的試用價值有限，不太可能拿來辦真實活動，
> 但仍建議做 Email 去重，避免同一人反覆索取。

---

## 寄出的信件內容

- 試用授權碼
- **明確標示有效時段**（起訖時間，台灣時間）— 這點務必顯眼，避免誤用
- 軟體下載連結：`https://guestars.app/drinkcam/download`
- 快速上手 3 步驟（安裝 → 貼授權碼 → 手機連同一 Wi-Fi）
- 提醒：**取得憑證需要網路，且手機與電腦要在同一個 Wi-Fi**
- 底部放購買連結 `https://guestars.app/drinkcam/buy`（試用完直接轉單）

---

## 同時通知老闆

有人申請試用時推播/寄信通知（含名稱、Email、預計試用時間、場地），
這是熱線索，可以主動跟進。可沿用付款通知的 LINE 推播機制。

---

## 視覺

沿用 `drinkcam_buy_設計規範.md`（米色暖橘設計系統），與購買頁一致。

---

## 官網文案已同步更新

原文案「我會在 24 小時內把試用授權碼寄到你的 Email」
已改為「**填完表單立即寄到你的 Email**」，請確保實作為即時發送。
